<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marek Cieloch - Portfolio</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">

</head>

<body>
    <header class="d-flex justify-content-center py-3 sticky-header">
        <ul class="nav nav-pills">
            <li class="nav-item"><a href="/index" class="nav-link active" aria-current="page">Home</a></li>
            <li class="nav-item"><a href="/projects" class="nav-link">Projects</a></li>
            <li class="nav-item"><a href="/contact" class="nav-link">Contact</a></li>
        </ul>
    </header>




    <h1>Employee Management</h1>


    <div class=".center-body">
        <!-- Button to create a new employee if the list is empty -->
        <% if (employees.length===0) { %>
            <p>No employees found. Click the button below to create a new employee.</p>
            <button onclick="location.href='/projects/employees/new'">Create New Employee</button>
            <% } else { %>
                <!-- Display the list of employees in a table -->
                <table border="1">
                    <thead>
                        <tr>
                            <th>Full Name</th>
                            <th>Date of Birth</th>
                            <th>Job Title</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% employees.forEach(employee=> { %>
                            <tr>
                                <td>
                                    <%= employee.fullName %>
                                </td>
                                <td>
                                    <%= new Date(employee.dateOfBirth).toISOString().slice(0, 10) %>
                                </td>
                                <td>
                                    <%= employee.jobTitle %>
                                </td>
                                <td>
                                    <%= employee.email %>
                                </td>
                                <td>
                                    <%= employee.phone %>
                                </td>
                                <td>
                                    <!-- Edit Button -->
                                    <button
                                        onclick="location.href='/projects/employees/edit/<%= employee._id %>'">Edit</button>

                                    <!-- Delete Button -->
                                    <form action="/projects/employees/delete/<%= employee._id %>" method="POST"
                                        style="display:inline;">
                                        <button type="submit"
                                            onclick="return confirm('Are you sure you want to delete this employee?')">Delete</button>
                                    </form>

                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
                <% } %>

    </div>

    <div id="footer"></div>
</body>


<script src="/js/bootstrap.bundle.min.js"></script>

<script src="js/scripts.js"></script>


</html>